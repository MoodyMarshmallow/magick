## Review Plan

1. Review the current uncommitted diff (`git diff`, `git diff --cached`, `git status --short`).
2. Read the full modified files to validate runtime behavior in context, especially local workspace rename flows across desktop, browser fallback, and web UI state.
3. Report only verified findings from changed code.

## Findings To Report

1. `apps/desktop/src/main/localWorkspaceService.ts`: `resolveAvailableSiblingFileName()` only considers sibling files, so renaming a file to a name already used by a sibling directory still throws instead of applying the new numeric-suffix collision policy.
2. `packages/shared/src/localWorkspace.test.ts`: the new shared helper test file is outside the Vitest include globs in `vitest.config.ts`, so these tests will not run under `bun run test`.

## Verification

1. Confirm the file-collision finding by tracing `renameFile()` -> `resolveAvailableSiblingFileName()` -> `resolveWorkspaceSiblingPath()`.
2. Confirm the test-discovery finding by comparing `packages/shared/src/localWorkspace.test.ts` against `vitest.config.ts` `test.include`.
