## Goal

Review the current uncommitted workspace-file changes with a bug-first lens and produce actionable findings only for modified code.

## Review Focus

1. Verify the end-to-end file rename flow across shared helpers, desktop service, browser fallback client, file tree UI, and workspace surface UI.
2. Check whether the new title-stem behavior introduces incorrect filename generation or mismatches between displayed titles and persisted paths.
3. Confirm that tree refresh/remount behavior in `AppShell` does not mask stale state bugs in rename/create/delete flows.

## Critical Files To Inspect

- `packages/shared/src/localWorkspace.ts`
- `apps/desktop/src/main/localWorkspaceService.ts`
- `apps/web/src/features/workspace/data/localWorkspaceFileClient.ts`
- `apps/web/src/features/workspace/tree/FileTree.tsx`
- `apps/web/src/features/workspace/components/WorkspaceSurface.tsx`
- `apps/web/src/app/AppShell.tsx`
- Supporting tests around the same paths

## Expected Findings Format

- Severity first
- File and line references
- Concrete reproduction scenario
- Why the behavior is incorrect

## Verification

1. Compare UI rename inputs against backend/client sanitization rules.
2. Cross-check changed tests to see which prior behaviors were intentionally removed versus accidentally regressed.
3. Ensure any reported issue is reproducible from the current diff alone and is not pre-existing behavior.
